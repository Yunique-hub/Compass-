# 应用层长期记忆约定

精确目标和时间预算进入结构化存储；偏好、事件和成就可进入语义存储。任何存取都必须携带 `user_id`。自动长期记忆属于 Agent 应用层的条件启用能力，不是大模型本身或未知平台的原生永久记忆。

写入顺序：候选提取 → 用户意愿 → 敏感检查 → 重复检查 → 冲突检查 → 可信度 → 存储类型。删除必须覆盖结构化记录、向量记录、索引、缓存和临时副本；审计只保留动作、时间、原因与版本，不保留被删原文。
